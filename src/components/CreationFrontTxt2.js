import React from 'react';

export const CreationFrontTxt2 = (props) => {
    let {meaningsList, name, pos, deleteMeaning} = props;
    return(
        <div className="creation-front-txt">
            {name}
        </div>
    )
};