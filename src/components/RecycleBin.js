import React from 'react';

export const RecycleBin = (props) => {
    return(
        <div className = "recycle-bin-container">
            <button className="recycle-bin-btn">
                <span recycle-bin-btn-txt>Recycle bin</span>
            </button>
        </div>
    );
};